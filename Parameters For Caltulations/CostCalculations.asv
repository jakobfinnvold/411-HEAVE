clear; close all; clc; 

%% Motor
omega_m = 1.5;
PM_max = 200e3;
poleselect = [2; 4; 6; 8]; % Number of poles
n_p = poleselect(3,1);
PM = 160e3;
CM = omega_m * (1 + (PM/PM_max) + abs(n_p - 4)/4); 


%% Drive
C_c = omega_c

%% GB

